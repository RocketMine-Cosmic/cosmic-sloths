{
  "name": "PlayerSave",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string"
    },
    "save_data": {
      "type": "object",
      "additionalProperties": true
    },
    "updated_at": {
      "type": "number"
    }
  },
  "required": [
    "user_id",
    "save_data"
  ],
  "rls": {
    "create": {
      "data.user_id": "{{user.id}}"
    },
    "read": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}